#include <cstdio>

const int N = 1100;

int T, n, m;
char tmp_read;;
int height[N], width[N];
bool map[N][N];

inline void init(){
	
}

inline void readin(){
	scanf("%d%d", &n, &m);
	for(int i = 1; i <= n; i++){
		for(int j = 1; j <= m; j++){
			
		}
	}
}

inline void get_ans(){

}

int main(){
	scanf("%d", &T);
	while(T--){
		init();
		readin();
		get_ans();
	}
}
