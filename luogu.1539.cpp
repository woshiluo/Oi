#include <cstdio>

int n, m;
int map[N][N];
// 0 '0'
// 1 '1'
// 2 '.'

int main() {
	scanf( "%d%d", &n, &m );
	for( int i = 1; i <= n; i ++ ) {
		char str[N];
		scanf( "%s", str + 1 );
		for( int j = 1; j <= m; j ++ ) {
		}
	}
}
