#include <cstdio>
#include <cstring>

#include <algorithm>

const int N = 1100;
const int H = 50;

int f[2][2][H][H][H];

int main() {
	int n, h;
	int cur = 1; la = 0;
	f[la][1][0][0][0] = 1;
	for( int i = 1; i <= n; i ++ ) {
		for( int a1 = 0; a1 <= h; a1 ++ ) {
		for( int a2 = 0; a2 <= h; a2 ++ ) {
		for( int a3 = 0; a3 <= h; a3 ++ ) {
			for( int j = 0; j <= 1; j ++ ) {
			}
		}
		}
		}
	}
}
